# 21 — Container build flow

> Spec status: **DRAFT (stub)**. Normative spec for the Containerfile layer
> ordering. The implementation lives in
> [`../../container/Containerfile`](../../container/Containerfile).

## Stage ordering

| Stage (`FROM ... AS`) | Layer | Purpose | Inputs |
|---|---|---|---|
| `base`            | 1 | minimum packages required for `chezmoi apply` (base-devel, sudo, curl, git). | `manjarolinux/base:latest`, `dependencies/layer_1.txt` |
| `no-config-base`  | 1 | remap the `builder` user to host uid/gid for bind-mount ownership. | `HOST_UID`, `HOST_GID` build-args |
| _(planned)_ `chezmoi-apply` | 2 | run `chezmoi apply` once with secrets injected via BuildKit `--secret` mounts. | `BW_ID` secret, `/var/lib/chezmoi-source` bind |
| _(planned)_ `tooling` | 3+ | install per-layer tool sets (`paru`, `nix`, `mise`, `uv`) per `dependencies/layer_<N>.txt`. | layer txt files |

## Invariants

- I1: every stage is built from a **named** `AS <stage>` so downstream stages can `COPY --from=<stage>` without coupling to a layer ordinal.
- I2: the `builder` user is the only non-root account inside the image; `USER builder` is set before any installation step that does not require root.
- I3: each layer txt file is regenerated by `make gen-deps` from `dependencies/packages.toml` — hand-editing layer txt files is forbidden.

> Secret handling (BuildKit `--mount=type=secret`, no `COPY`/`ARG`) is
> delegated to [`20-container-rules.md`](20-container-rules.md) I4 — that
> invariant covers both build-time and runtime, so this spec does not
> restate it.

> NOTE: an earlier draft included a `safe.directory` invariant for
> `/var/lib/chezmoi-source`. It was removed in sync with
> [`20-container-rules.md`](20-container-rules.md) — `--userns=keep-id`
> already keeps the host UID inside the container, so git's
> "dubious ownership" check does not fire in the supported run mode.

## Acceptance criteria

A new stage may land only when:

1. its name appears in this spec under "Stage ordering"
2. its `dependencies/layer_<N>.txt` is generated, not hand-written
3. the corresponding [`02-installed-programs.md`](02-installed-programs.md) entries are reachable from `packages.toml`
4. invariants I1–I3 hold after the change

## Open questions

- Q1: how are AUR packages installed without an interactive sudo password inside the build? `--mount=type=bind,from=...` of a pacman cache, or `makepkg --noconfirm`?
